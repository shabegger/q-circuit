@{
    Layout = null;
}

<!DOCTYPE html>

<html>
  <head>
    <meta name="viewport" content="width=device-width" />
    <title>Quantum Circuit Builder</title>

    @Styles.Render("~/Content/quantum")
  </head>
  <body>
    <div id="quantum-container"></div>

    @Scripts.Render("~/bundles/svg")
    @Scripts.Render("~/bundles/quantum")
    <script>
      ; (function (window, SVG) {

        'use strict';

        var circuitBuilder;

        if (!SVG.supported) {
          window.document.getElementById('quantum-container').innerText =
            'SVG is not supported. Please use a modern browser.';
          return;
        }

        circuitBuilder = new Quantum.CircuitBuilder('quantum-container', 6, [
          Quantum.Gate,
          Quantum.Gate,
          Quantum.Gate,
          Quantum.Gate,
          Quantum.Gate
        ]);

      }(this, this.SVG));
    </script>
  </body>
</html>
